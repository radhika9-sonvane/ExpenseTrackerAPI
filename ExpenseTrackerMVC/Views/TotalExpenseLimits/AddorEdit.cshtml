@model ExpenseTrackerMVC.Models.mvcTotalExpenseLimits
@{
    ViewBag.Title = "AddorEdit";
}

<div class="form-body">
    @using (Html.BeginForm())
    {
        @Html.HiddenFor(model=>model.totalexpenseid)
        <div class="form-group">
            Total Expense Limit
            @Html.EditorFor(model=>model.totalexpenselimit1)
            @Html.ValidationMessageFor(model=>model.totalexpenselimit1)
        </div>
        <div class="form-group">
            <input type="submit" value="Submit" class="btn button" />
            <input type="reset" value="Reset" class="btn button" />
        </div>
     }
</div>

